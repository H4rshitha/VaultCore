apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-deployment
  labels:
    app: ledger-service
    app.kubernetes.io/name: ledger-service
    app.kubernetes.io/part-of: vaultcore-platform
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: ledger-service
  template:
    metadata:
      labels:
        app: ledger-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ledger-service
          image: vaultcore/ledger-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3004
          env:
            - name: PORT
              value: '3004'
          envFrom:
            - configMapRef:
                name: vaultcore-config
            - secretRef:
                name: vaultcore-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  labels:
    app: ledger-service
    app.kubernetes.io/name: ledger-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3004
      targetPort: 3004
  selector:
    app: ledger-service
